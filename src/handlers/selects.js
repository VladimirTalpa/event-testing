
module.exports = async function handleSelects(interaction) {
  // пока не используем select-меню в Part 2
  return interaction.reply({ content: "Select menu is not used yet.", ephemeral: true });
};
